on: [ 'push' ]

jobs:
  nyrah-build:
    runs-on: ubuntu-latest

    steps:
      - name: Update branch to latest
        uses: actions/checkout@v2
      - name: Set up go
        uses: actions/setup-go@v2
        with:
          go-version: ^1.16.3
      - name: Download dependencies
        working-directory: ${{ github.workspace }}/src
        run: go get -v -d all
      - name: Build go project
        working-directory: ${{ github.workspace }}/src
        run: go build
      - name: deub
        run: echo -e "Chave >$KEY< Hosts >$KNOWN_HOSTS<"
        env:
          KEY: ${{ secrets.DEDICATED_1_SSH_KEY }}
          KNOWN_HOSTS: ${{ secrets.KNOWN_HOSTS }}
      - name: Set up SSH key
        uses: shimataro/ssh-key-action@v2
        with:
          KEY: ${{ secrets.DEDICATED_1_SSH_KEY }}
          KNOWN_HOSTS: ${{ secrets.KNOWN_HOSTS }}
      - name: Deploy to all servers
        run: scp $DIRECTORY/src/nyrah root@149.56.242.214:/root
        env:
          DIRECTORY: ${{ github.workspace }}
#        uses: nogsantos/scp-deploy@master
#        with:
#          src: ${{ github.workspace }}/src/nyrah
#          remote: /root
#          host: ${{ secrets.DEDICATED_1_HOST_ADDRESS }}
#          user: ${{ secrets.DEDICATED_1_HOST_USER }}
#          port: ${{ secrets.DEDICATED_1_PORT }}
#          key: ${{ secrets.DEDICATED_1_SSH_KEY }}